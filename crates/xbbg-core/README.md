# xbbg-core

Core Bloomberg API abstractions for xbbg.

This crate provides low-level wrappers around the Bloomberg C++ SDK, including session management, request building, and response parsing.

## Features

- Safe Rust wrappers over blpapi-sys FFI
- Session and service management
- Request/response types
- Element and message parsing
