[env]
XBBG_DEV_SDK_ROOT = { value = "vendor/blpapi/blpapi_cpp", relative = true }
# Note: PYO3_PYTHON is not set here - PyO3 auto-detects the Python interpreter.
# For local dev with uv venv, activate the venv or set PYO3_PYTHON manually.
