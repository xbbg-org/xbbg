---
title: xbbg
description: Public documentation for the xbbg Python package and @xbbg/core Node.js package
template: splash
hero:
  tagline: Python-first Bloomberg APIs powered by a shared Rust engine
  image:
    file: ../../assets/xbbg-logo.png
  actions:
    - text: Python package
      link: /xbbg/python/
      icon: right-arrow
    - text: Node.js package
      link: /xbbg/javascript/
      icon: right-arrow
      variant: minimal
    - text: Package Map
      link: /xbbg/overview/package-map/
      icon: document
      variant: minimal
    - text: GitHub
      link: https://github.com/alpha-xone/xbbg
      icon: external
      variant: minimal
---

import { Card, CardGrid, Tabs, TabItem } from '@astrojs/starlight/components';
import {
  PhBracketsCurly,
  PhCpu,
  PhGitBranch,
  PhPackage,
} from 'phosphor-icons-astro';

## Public packages

This site documents the public surfaces of the xbbg monorepo: the production Python package, `xbbg`, and the published Node.js package, `@xbbg/core`. Internal Rust crates, private browser packages, and platform-specific packaging artifacts stay out of the public navigation on purpose.

<CardGrid stagger>
  <Card title="xbbg (Python)">
    <div style="margin-bottom: 0.75rem; color: var(--sl-color-accent-high);">
      <PhPackage size={20} weight="duotone" aria-hidden="true" />
    </div>
    The primary public package: Bloomberg requests, streaming, async support, and backend selection for pandas, Polars, PyArrow, and more.
  </Card>
  <Card title="@xbbg/core (Node.js)">
    <div style="margin-bottom: 0.75rem; color: var(--sl-color-accent-high);">
      <PhBracketsCurly size={20} weight="duotone" aria-hidden="true" />
    </div>
    Direct Node.js access to the same Rust engine. The package is public today, but still marked experimental alpha while the API matures.
  </Card>
  <Card title="Shared Rust engine">
    <div style="margin-bottom: 0.75rem; color: var(--sl-color-accent-high);">
      <PhCpu size={20} weight="duotone" aria-hidden="true" />
    </div>
    Python and Node.js both ride on the same Rust core for transport, Arrow-native data handling, and Bloomberg session management.
  </Card>
  <Card title="Monorepo-aware docs">
    <div style="margin-bottom: 0.75rem; color: var(--sl-color-accent-high);">
      <PhGitBranch size={20} weight="duotone" aria-hidden="true" />
    </div>
    The navigation is organized around public packages and language surfaces instead of repo folders, so the docs scale with the monorepo.
  </Card>
</CardGrid>

## Quick examples

<Tabs>
  <TabItem label="Python">
    ```python
    import xbbg

    xbbg.configure(host='localhost', port=8194)
    prices = xbbg.bdp(
        ['AAPL US Equity', 'MSFT US Equity'],
        ['PX_LAST', 'VOLUME'],
    )
    ```
  </TabItem>
  <TabItem label="Node.js">
    ```ts
    import * as xbbg from '@xbbg/core';

    xbbg.configure({ host: 'localhost', port: 8194 });
    const prices = await xbbg.blp.abdp(
        ['AAPL US Equity'],
        ['PX_LAST', 'SECURITY_NAME'],
    );
    ```
  </TabItem>
</Tabs>

The docs use top-level Python helpers like `xbbg.bdp(...)`. If you already use the module-style import from the repository README, `from xbbg import blp` and `blp.bdp(...)` continue to work too.

Use the [package map](/xbbg/overview/package-map/) when you need to choose the right public package for a workflow, or jump straight into the [Python docs](/xbbg/python/) or [JavaScript docs](/xbbg/javascript/).