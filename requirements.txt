# NOTE: This file is deprecated.
# Development now uses pyproject.toml + uv. To install, run:
#   uv sync --locked
# Or to add dependencies, use:
#   uv add <package>
-i https://pypi.org/simple
--extra-index-url https://bcms.bloomberg.com/pip/simple/
numpy >= 1.15.0
pandas >= 1.0.0, <=1.5.3
pyarrow >= 1.0.1
pytz >= 2020.4
ruamel.yaml >= 0.15.0
pytest
