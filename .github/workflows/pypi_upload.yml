name: Upload Python Package

on:
  push:
    tags:
      - 'v*'  # Match all version tags

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    # Only upload stable releases to PyPI (pre-releases go to TestPyPI)
    # Pre-releases have format vX.Y.ZaN, vX.Y.ZbN, or vX.Y.ZrcN
    # Skip if tag matches pre-release pattern (contains a/b/rc after version numbers)
    if: ${{ !contains(github.ref_name, 'a') && !contains(github.ref_name, 'b') && !contains(github.ref_name, 'rc') }}

    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - name: Install uv
      shell: bash
      run: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
        echo "$HOME/.local/bin" >> "$GITHUB_PATH"
    - name: Create uv virtual environment
      run: |
        uv venv
    - name: Build distributions with uv
      run: |
        uv build
    - name: Publish to PyPI via OIDC
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: dist/
        skip-existing: true
