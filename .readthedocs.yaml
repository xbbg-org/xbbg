version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    build:
      html:
        - mkdir -p "$READTHEDOCS_OUTPUT/html"
        - cp index.html "$READTHEDOCS_OUTPUT/html/index.html"
        - cp 404.html "$READTHEDOCS_OUTPUT/html/404.html"
